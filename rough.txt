if (
  (this.startDate! >= element.startdate && this.startDate! <= element.enddate) ||
  (this.endDate! >= element.startdate && this.endDate! <= element.enddate) ||
  (this.startDate! <= element.startdate && this.endDate! >= element.enddate)
) {
  this.isSlotBooked = true;
  return;
}
